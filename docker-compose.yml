# Generated by Auto (Composer) - 2026-05-30
# Model: Composer
# Local test: build context needs storage-lib/ (clone beside photoframe or use CI build).
services:
  photoframe:
    build:
      context: .
      dockerfile: Dockerfile
    image: photoframe:local
    ports:
      - "8082:8082"
    env_file:
      - path: .env
        required: false
    environment:
      PHOTOFRAME_HOME: /app
      PHOTOFRAME_STORAGE_PROVIDER: ${PHOTOFRAME_STORAGE_PROVIDER:-filesystem}
      PHOTOFRAME_STORAGE_ROOT: /app/data
    volumes:
      - photoframe-data:/app/data
      - photoframe-logs:/app/logs
    restart: unless-stopped

volumes:
  photoframe-data:
  photoframe-logs:
