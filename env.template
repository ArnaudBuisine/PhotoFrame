# Generated by Auto (Composer) - 2026-05-30
# Copy to .env for local docker compose (do not commit .env).

# --- App ---
SERVER_PORT=8082
PHOTOFRAME_HOME=/app
LOG_LEVEL=INFO
LOGGING_FILE_NAME=/app/logs/photoframe.log
photoframe.source=google

# --- Blob storage (storage-lib) ---
# filesystem | azure
PHOTOFRAME_STORAGE_PROVIDER=filesystem
PHOTOFRAME_STORAGE_ROOT=/app/data
PHOTOFRAME_STORAGE_GOOGLE_PREFIX=google-photos-cache
# When PHOTOFRAME_STORAGE_PROVIDER=azure:
# AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=...
# PHOTOFRAME_STORAGE_AZURE_CONTAINER=photoframe

# --- Google Photos OAuth (secure — use Azure --secure-environment-variables in production) ---
GOOGLE_PHOTOS_CLIENT_ID=
GOOGLE_PHOTOS_CLIENT_SECRET=
GOOGLE_PHOTOS_REFRESH_TOKEN=
